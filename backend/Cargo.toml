[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
models = { path = "../models" }
runner = { path = "../runner" }
clap = { version = "4.3.10", features = ["derive"] }
