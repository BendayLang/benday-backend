[workspace]
members = ["backend", "models", "runner"]
default-members = ["backend", "models"]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
models = { path = "models" }
runner = { path = "runner" }
